const config = {
    defaultSettings: {
        prefix: "%",
        econLogChannel: "discord-economics",
        modLogChannel: "mod-log",
        systemNotice: "true",
        welcomeChannel: "welcome",
        welcomeMessage:
          "Say hello to {{user}}, everyone! We all need a warm welcome sometimes :D",
        welcomeEnabled: "false",
        leaveMessage: "{{user}} left... I liked them :SayoriCry:",
        leaveEnabled: "false",
    },
};

module.exports = config;